type Gamemodes = "rps";

class GameUtils {
  // Set player to a game.
  public static async startGame(game: Gamemodes, channel: string, players: string[]) {
    
  }

  // Check if player is in a game.
  public static async isPlayerPlaying(channel: string, player: string) {

  }
}

export default GameUtils;